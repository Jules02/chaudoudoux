<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <div class="signinpop-content">
        <p class="signinpop-info">Vous avez normalement déjà reçu vos codes d’accès via NEO. Si ce n'est pas le cas ou si vous n'avez pas accès à NEO, contactez la MDL via <a href="https://www.instagram.com/mdleuro/">Instagram (@mdleuro)</a> ou par <a href="mailto:julesdupont02@gmail.com">mail</a></p>

        <input placeholder="Nom d'utilisateur" type="text" value="{{ last_username }}" name="username" id="inputUsername" class="form-input" required autofocus>
        <input placeholder="Mot de passe" type="password" name="password" id="inputPassword" class="form-control form-input" required>

        <input type="hidden" name="_csrf_token"
               value="{{ csrf_token('authenticate') }}"
        >

    <button class="signinpop-btn btn btn-blue" type="submit">Connexion</button>
    </div>
</form>
